/**
 * Header offset height in pixels.
 *
 * @constant {number}
 * @default 57
 */
export const HEADER_OFFSET_PX = 57;

/**
 * Minimum pixel threshold to ignore micro-drags (accidental clicks).
 * Use this for tiny pointer movement tolerances.
 * @constant {number}
 * @default 2
 */
export const MIN_DRAG_THRESHOLD_PX = 2;
